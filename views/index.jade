extends layout

block content
  h1= title
  p Welcome to #{title}
  button(name="#{loginText}", onclick='location.href= "#{loginRoute}"') #{loginText}
  button(name="#{registerText}", onclick='location.href= "#{registerRoute}"') #{registerText}
